You are an expert Next.js, Tailwind CSS, and Framer Motion developer. I am building a hackathon project called "Live Match Tactician" (an AI Cricket Strategy Agent). 
Please set up the entire Hero UI section in one go. Follow these exact steps and generate the necessary code for my Next.js App Router project:

STEP 1: DEPENDENCIES & SETUP
- Ensure the project has `framer-motion`, `lucide-react`, `clsx`, and `tailwind-merge` installed. Provide the exact npm install command.
- Ensure the `components/ui` folder exists.
- Update `next.config.mjs` (or `next.config.js`) to allow images from `images.unsplash.com`.

STEP 2: CREATE THE COMPONENT FILE
Create `components/ui/container-scroll-animation.tsx` and paste this exact code:

"use client";
import React, { useRef } from "react";
import { useScroll, useTransform, motion, MotionValue } from "framer-motion";

export const ContainerScroll = ({
  titleComponent,
  children,
}: {
  titleComponent: string | React.ReactNode;
  children: React.ReactNode;
}) => {
  const containerRef = useRef<HTMLDivElement>(null);
  const { scrollYProgress } = useScroll({ target: containerRef });
  const [isMobile, setIsMobile] = React.useState(false);

  React.useEffect(() => {
    const checkMobile = () => setIsMobile(window.innerWidth <= 768);
    checkMobile();
    window.addEventListener("resize", checkMobile);
    return () => window.removeEventListener("resize", checkMobile);
  }, []);

  const scaleDimensions = () => (isMobile ? [0.7, 0.9] : [1.05, 1]);
  const rotate = useTransform(scrollYProgress, [0, 1], [20, 0]);
  const scale = useTransform(scrollYProgress, [0, 1], scaleDimensions());
  const translate = useTransform(scrollYProgress, [0, 1], [0, -100]);

  return (
    <div className="h-[60rem] md:h-[80rem] flex items-center justify-center relative p-2 md:p-20 bg-black" ref={containerRef}>
      <div className="py-10 md:py-40 w-full relative" style={{ perspective: "1000px" }}>
        <Header translate={translate} titleComponent={titleComponent} />
        <Card rotate={rotate} translate={translate} scale={scale}>{children}</Card>
      </div>
    </div>
  );
};

export const Header = ({ translate, titleComponent }: any) => {
  return (
    <motion.div style={{ translateY: translate }} className="div max-w-5xl mx-auto text-center">
      {titleComponent}
    </motion.div>
  );
};

export const Card = ({
  rotate, scale, children,
}: {
  rotate: MotionValue<number>; scale: MotionValue<number>; children: React.ReactNode;
}) => {
  return (
    <motion.div
      style={{
        rotateX: rotate, scale,
        boxShadow: "0 0 #0000004d, 0 9px 20px #0000004a, 0 37px 37px #00000042, 0 84px 50px #00000026, 0 149px 60px #0000000a, 0 233px 65px #10b981",
      }}
      className="max-w-5xl -mt-12 mx-auto h-[30rem] md:h-[40rem] w-full border-4 border-[#1f2937] p-2 md:p-6 bg-[#111827] rounded-[30px] shadow-2xl"
    >
      <div className="h-full w-full overflow-hidden rounded-2xl bg-zinc-900 md:rounded-2xl md:p-4 relative">
        {children}
      </div>
    </motion.div>
  );
};

STEP 3: CREATE THE MAIN PAGE
Overwrite `app/page.tsx` with this code to render the Cricket AI dashboard:

"use client";
import React from "react";
import { ContainerScroll } from "@/components/ui/container-scroll-animation";
import Image from "next/image";
import { Activity, Mic } from "lucide-react";

export default function HeroScrollDemo() {
  return (
    <div className="flex flex-col overflow-hidden pb-[500px] pt-[200px] bg-black min-h-screen">
      <ContainerScroll
        titleComponent={
          <>
            <div className="flex items-center justify-center gap-3 mb-4">
              <span className="px-4 py-1 rounded-full bg-emerald-500/10 text-emerald-500 border border-emerald-500/20 text-sm font-medium flex items-center gap-2">
                <span className="w-2 h-2 rounded-full bg-emerald-500 animate-pulse"></span>
                Gemini 2.0 Live Agent
              </span>
            </div>
            <h1 className="text-4xl md:text-5xl font-semibold text-white">
              Real-Time Strategy for <br />
              <span className="text-5xl md:text-[6rem] font-bold mt-1 leading-none text-transparent bg-clip-text bg-gradient-to-r from-emerald-400 to-cyan-500">
                Live Match Tactician
              </span>
            </h1>
          </>
        }
      >
        <div className="relative h-full w-full">
          <Image
            src="https://images.unsplash.com/photo-1531415074968-036ba1b575da?q=80&w=2069&auto=format&fit=crop"
            alt="Cricket Stadium Dashboard"
            fill
            className="rounded-2xl object-cover opacity-60"
            draggable={false}
          />
          <div className="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent flex flex-col justify-end p-8">
            <div className="bg-black/60 backdrop-blur-md border border-white/10 rounded-xl p-6 max-w-lg">
              <div className="flex items-center gap-4 mb-4">
                <div className="w-12 h-12 rounded-full bg-emerald-500/20 flex items-center justify-center border border-emerald-500/50">
                  <Mic className="text-emerald-400 w-6 h-6" />
                </div>
                <div>
                  <h3 className="text-white font-semibold">Voice Coach Active</h3>
                  <p className="text-emerald-400 text-sm flex items-center gap-1">
                    <Activity className="w-3 h-3" /> Analyzing pitch data...
                  </p>
                </div>
              </div>
              <p className="text-gray-300 text-sm leading-relaxed">
                "Based on the live data from RapidAPI, the pitch is offering 2.4 degrees of turn. Bringing in your off-spinner now increases your win probability by 18% against the left-handed batsman."
              </p>
            </div>
          </div>
        </div>
      </ContainerScroll>
    </div>
  );
}

Please execute all steps, write the files, and confirm when the UI is ready to preview.